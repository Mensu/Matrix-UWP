<UserControl
  x:Class="Matrix_UWP.UserControls.InfoMessage"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:local="using:Matrix_UWP.UserControls"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  mc:Ignorable="d"
  d:DesignHeight="300"
  d:DesignWidth="400"
  >
  <ContentControl HorizontalAlignment="Center" VerticalAlignment="Center">
    <Grid x:Name="Container" Background="#449d44">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto"/>
        <ColumnDefinition Width="Auto"/>
      </Grid.ColumnDefinitions>
      <TextBlock x:Name="Message" Foreground="White" Grid.Column="0" Text="Haha，一定是你忘了初始化文本" Padding="12" FontSize="23" TextAlignment="Center" VerticalAlignment="Center"/>
      <Button x:Name="Close" Content="✕" Foreground="White" Background="Transparent" Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Top" Height="40" Width="40" Click="Close_Click"/>
    </Grid>
  </ContentControl>
</UserControl>
